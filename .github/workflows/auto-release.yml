name: Auto Release

# On every merge to `main`, cut a SemVer release automatically:
#   1. Derive the bump (major/minor/patch) from the Conventional Commits
#      since the last tag.
#   2. Create and push the new `vX.Y.Z` tag.
#   3. Move the major-version alias tag (e.g. `v2`) to the new release so
#      consumers pinning `@v2` pick it up — the standard Marketplace pattern.
#   4. Publish a GitHub Release with auto-generated notes.
#
# Mirrors the DailyBot `auto-release` convention (see the agent-skill repo),
# adapted for a Marketplace-published composite action: there is no npm
# publish or build artefact — the "package" is the tagged source itself.
#
# Bootstrapping: the very first release (v1.0.0) is cut MANUALLY so it can be
# published to the GitHub Marketplace from the Release UI. This workflow skips
# while no tag exists yet, then takes over for every release after that.

on:
  push:
    branches: [main]

concurrency:
  group: auto-release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  auto-release:
    name: 'Auto Release on Merge to Main'
    runs-on: ubuntu-latest
    # Skip release-bookkeeping commits and explicit opt-outs.
    if: >-
      !startsWith(github.event.head_commit.message, 'chore(release):') &&
      !contains(github.event.head_commit.message, '[skip release]')
    steps:
      - name: Step 0 - 📥 Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.AUTOMATION_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Step 1 - ⚙️ Configure Git
        run: |
          git config user.name "🤖 DailyBot"
          git config user.email "ops@dailybot.com"

      - name: Step 2 - 📝 Determine version bump from commits
        id: version
        run: |
          set -euo pipefail
          # Use the highest SemVer RELEASE tag (vX.Y.Z), NOT
          # `git describe --tags --abbrev=0`. `describe` walks by ancestry and
          # returns the most recent reachable tag — which, once the moving `v2`
          # major-alias tag is force-moved onto the latest release commit, is
          # `v2` itself. That parses as MAJOR=2, MINOR="" (→0) and a minor bump
          # produces the already-existing `v2.1.0` (fatal: tag already exists).
          # Filtering to exact `vMAJOR.MINOR.PATCH` and version-sorting ignores
          # the `v1`/`v2` aliases and any pre-release/suffix tags.
          LATEST_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname \
            | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -n1 || echo "")
          if [ -z "$LATEST_TAG" ]; then
            echo "No existing tag found. The first release (v1.0.0) must be cut"
            echo "manually so it can be published to the Marketplace from the"
            echo "Release UI. Skipping auto-release until a base tag exists."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "Latest tag: $LATEST_TAG"

          CURRENT_VERSION=${LATEST_TAG#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"

          COMMITS=$(git log "${LATEST_TAG}..HEAD" --pretty=format:"%s" 2>/dev/null || echo "")
          echo "Commits since $LATEST_TAG:"
          echo "$COMMITS"

          if [ -z "$COMMITS" ]; then
            echo "No new commits since $LATEST_TAG — nothing to release."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Conventional-commit → bump mapping.
          BUMP="patch"
          if echo "$COMMITS" | grep -qE "^(feat|feature|fix|perf|refactor)(\(.+\))?!:|BREAKING CHANGE"; then
            BUMP="major"
          elif echo "$COMMITS" | grep -qE "^(feat|feature)(\(.+\))?:"; then
            BUMP="minor"
          elif echo "$COMMITS" | grep -qE "^(fix|perf)(\(.+\))?:"; then
            BUMP="patch"
          fi
          echo "Bump type: $BUMP"

          case "$BUMP" in
            major) MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR + 1)); PATCH=0 ;;
            patch) PATCH=$((PATCH + 1)) ;;
          esac

          NEW_VERSION="v${MAJOR}.${MINOR}.${PATCH}"
          MAJOR_TAG="v${MAJOR}"
          echo "New version: $NEW_VERSION (major alias: $MAJOR_TAG)"
          {
            echo "skip=false"
            echo "new_version=$NEW_VERSION"
            echo "major_tag=$MAJOR_TAG"
            echo "previous_tag=$LATEST_TAG"
          } >> "$GITHUB_OUTPUT"

      - name: Step 2.5 - 📦 Sync skill artifacts to the new version
        # The action ships `prompts/default.md`; the local companion skill
        # (`skills/ai-diff-reviewer/`) ships its own byte-copy alongside a
        # `version:` frontmatter field that must equal the action tag for
        # `npx skills update` to detect a new release. This step keeps
        # both invariants (byte-copy identical + version equal) true at
        # every tag. The resulting `chore(release):` commit uses the
        # `[skip release]` marker so this workflow does NOT re-trigger on
        # its own push (the guard on the job's `if:` catches it).
        id: skill_sync
        if: steps.version.outputs.skip == 'false'
        run: |
          set -euo pipefail
          NEW_VERSION="${{ steps.version.outputs.new_version }}"
          VERSION_NO_V="${NEW_VERSION#v}"

          # Bump `version:` in every SKILL.md under skills/ (today: only
          # ai-diff-reviewer; loop makes it work when future sibling skills land).
          while IFS= read -r f; do
            sed -i "s/^version: \".*\"/version: \"${VERSION_NO_V}\"/" "$f"
          done < <(find skills -name 'SKILL.md')

          # Byte-copy the shipped prompt so the vendored skill stays
          # self-contained (Open Agent Skills convention).
          cp prompts/default.md skills/ai-diff-reviewer/prompt.md

          # Stamp the CHANGELOG: `## [Unreleased]` becomes `## [X.Y.Z] — date`
          # and a fresh empty [Unreleased] is opened. Idempotent; an empty
          # section is skipped with a notice (release still proceeds).
          python3 .github/scripts/stamp_changelog.py --version "${NEW_VERSION}" --date "$(date -u +%F)" CHANGELOG.md

          if git diff --quiet -- skills/ CHANGELOG.md; then
            echo "Skill artifacts and CHANGELOG already in sync with ${NEW_VERSION}; nothing to commit."
            echo "has_commit=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git add skills/ CHANGELOG.md
          git commit -m "chore(release): sync skill artifacts for ${NEW_VERSION} [skip release]"
          echo "has_commit=true" >> "$GITHUB_OUTPUT"

      - name: Step 3 - 🏷️ Create and push tags
        if: steps.version.outputs.skip == 'false'
        run: |
          set -euo pipefail
          NEW_VERSION="${{ steps.version.outputs.new_version }}"
          MAJOR_TAG="${{ steps.version.outputs.major_tag }}"
          HAS_SYNC_COMMIT="${{ steps.skill_sync.outputs.has_commit }}"

          git tag -a "$NEW_VERSION" -m "Release $NEW_VERSION"

          if [ "$HAS_SYNC_COMMIT" = "true" ]; then
            # Atomic push: EITHER the sync commit lands on main AND the tag
            # is created, OR neither happens. Without `--atomic`, a branch
            # push blocked by branch protection can still succeed on the
            # tag ref, leaving a partial-release state (tag exists on
            # remote, but the commit it points to only lives on the runner).
            # PR #26's initial release run demonstrated this exact failure
            # (see docs/RELEASE_RECOVERY.md § "Partial release: tag pushed
            # but sync commit rejected").
            if ! git push origin --atomic HEAD:main "$NEW_VERSION"; then
              echo "::error::Atomic push (main + $NEW_VERSION) rejected."
              echo "::error::Most likely cause: branch protection blocks the automation"
              echo "::error::token from pushing directly to main. See"
              echo "::error::docs/RELEASE_RECOVERY.md for both the immediate recovery"
              echo "::error::playbook and the AUTOMATION_GITHUB_TOKEN bypass configuration"
              echo "::error::that prevents this."
              exit 1
            fi
          else
            # No sync commit — HEAD equals origin/main, only the tag needs
            # pushing. Tag pushes are typically not gated by branch
            # protection rulesets that target branches.
            git push origin "$NEW_VERSION"
          fi

          # Move the major alias tag — `--force` is correct: it's a moving
          # pointer by design so `@v2` tracks the latest v2.x.y. Tag-only
          # push; not subject to branch protection.
          #
          # The `${NEW_VERSION}^{}` peel is load-bearing: without it,
          # `git tag -f $MAJOR_TAG $NEW_VERSION` creates a nested annotated
          # tag pointing at the $NEW_VERSION tag object (which then points
          # at the commit). Nested tags work but confuse tooling — a
          # consumer running `git checkout v2` gets a detached HEAD at the
          # tag object rather than the commit, and comparing tag-object
          # SHAs from `gh api …/git/refs/tags/v2` to commit SHAs never
          # matches. Peeling to `^{}` makes MAJOR_TAG a lightweight tag
          # directly at the commit. See docs/RELEASE_RECOVERY.md § "Move
          # the major alias tag" for the reasoning.
          git tag -f "$MAJOR_TAG" "${NEW_VERSION}^{}"
          git push origin "$MAJOR_TAG" --force

      - name: Step 3.5 - 🍽️ Dogfood — refresh the vendored ai-diff-reviewer skill
        # This repo dogfoods its own skill: `.agents/skills/ai-diff-reviewer/`
        # is a vendored copy pinned via `skills-lock.json`, installed exactly
        # the way any consumer would install it (`npx skills add`). Refreshing
        # right after we publish the new tag serves three purposes:
        #   1. Live smoke-test — proves the just-published tag actually
        #      installs cleanly via `npx skills`. If the install script or
        #      the tag layout is broken, the workflow fails HERE (loudly)
        #      instead of a consumer discovering it silently the next day.
        #   2. Keeps the vendored copy at HEAD equal to the latest release
        #      (rather than the release before the one we just cut), so any
        #      contributor cloning fresh gets the current skill by default.
        #   3. Refreshes `skills-lock.json` with the new content hash so
        #      restore/verify flows stay accurate.
        # Uses `[skip release]` in the commit message so this push does NOT
        # re-trigger the auto-release job.
        if: steps.version.outputs.skip == 'false'
        env:
          NEW_VERSION: ${{ steps.version.outputs.new_version }}
        run: |
          set -euo pipefail

          # `NEW_VERSION` is `vX.Y.Z`; the SKILL.md frontmatter and
          # `npx skills add @<ref>` want the bare `X.Y.Z` form.
          VERSION_NO_V="${NEW_VERSION#v}"

          # Give GitHub a moment to propagate the just-pushed tag through
          # the API (git tag list on the git protocol is instant; the
          # REST/graphql surface backing `npx skills` occasionally lags a
          # few seconds).
          sleep 5

          # Pin to the exact tag we just cut instead of letting `update`
          # pick "the newest matching tag". If the tag isn't visible yet
          # (propagation lag, publish failure), `npx skills add @<tag>`
          # fails loudly here — that's the whole point of the smoke test.
          # A silent "already up to date" from `update` would let a
          # broken release slip through, which is exactly the failure
          # mode the finding on the previous iteration of this step
          # flagged (PR #25 warning #1). Hard-pinning + `--force` keeps
          # the outcome deterministic even if the vendored tree is
          # already at some newer or equal state.
          #
          # Two separate `--yes` flags below — they mean different things
          # and both are required in a non-TTY (Actions) environment:
          #   * `npx --yes` — auto-installs the `skills` npm package
          #     without the "Ok to proceed?" download prompt.
          #   * `skills add ... -y` — auto-selects the default install
          #     targets (Claude Code, Codex, OpenCode) without the
          #     "Which agents do you want to install to?" interactive
          #     picker. Missing this flag caused the v1.6.1 release
          #     failure — the picker hung, `skills add` never wrote to
          #     `.agents/`, and the sanity check below correctly refused
          #     to commit the stale 1.6.0 snapshot.
          echo "::group::npx skills add DailybotHQ/ai-diff-reviewer@${NEW_VERSION} --skill ai-diff-reviewer --force -y"
          npx --yes skills add "DailybotHQ/ai-diff-reviewer@${NEW_VERSION}" \
            --skill ai-diff-reviewer --force -y
          echo "::endgroup::"

          # Explicit invariant assertion: the vendored SKILL.md's
          # `version:` field MUST equal the tag we just published. If it
          # doesn't, `npx skills` accepted the fetch but installed the
          # wrong content — fail hard rather than commit a
          # misrepresented "dogfood" state.
          VENDORED_VERSION=$(
            sed -nE 's/^version:[[:space:]]*"([^"]+)".*/\1/p' \
              .agents/skills/ai-diff-reviewer/SKILL.md | head -n1
          )
          if [ "${VENDORED_VERSION}" != "${VERSION_NO_V}" ]; then
            echo "::error::Vendored SKILL.md version is '${VENDORED_VERSION}'"
            echo "::error::but the just-published tag is '${NEW_VERSION}'"
            echo "::error::(expected frontmatter '${VERSION_NO_V}')."
            echo "::error::Refusing to commit a stale dogfood snapshot."
            exit 1
          fi
          echo "OK: vendored ai-diff-reviewer at ${NEW_VERSION} (verified)"

          # Empty diff after a `--force` add with a matching version
          # assertion means the working copy was already at this tag —
          # no commit needed (idempotent), but the smoke test PASSED
          # (the pinned fetch + version check both succeeded).
          if git diff --quiet -- .agents/skills/ai-diff-reviewer skills-lock.json; then
            echo "Vendored ai-diff-reviewer already at ${NEW_VERSION}; nothing to commit."
            exit 0
          fi

          git add .agents/skills/ai-diff-reviewer skills-lock.json
          git commit -m "chore(release): dogfood vendored ai-diff-reviewer to ${NEW_VERSION} [skip release]"
          git push origin HEAD:main

      - name: Step 4 - 📋 Generate release notes
        id: notes
        if: steps.version.outputs.skip == 'false'
        run: |
          set -euo pipefail
          PREV="${{ steps.version.outputs.previous_tag }}"
          NOTES=$(git log "${PREV}..${{ steps.version.outputs.new_version }}" --pretty=format:"- %s (%h)" 2>/dev/null || echo "")
          {
            echo "notes<<RELEASE_NOTES_EOF"
            echo "$NOTES"
            echo ""
            echo "**Full changelog:** [\`${PREV}...${{ steps.version.outputs.new_version }}\`](https://github.com/${{ github.repository }}/compare/${PREV}...${{ steps.version.outputs.new_version }})"
            echo "RELEASE_NOTES_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Step 5 - 🚀 Create GitHub Release
        if: steps.version.outputs.skip == 'false'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.new_version }}
          name: ${{ steps.version.outputs.new_version }}
          body: ${{ steps.notes.outputs.notes }}
          token: ${{ secrets.AUTOMATION_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
